# data

Repository-owned data assets.

- `raw/` is reserved for original inputs and immutable source artifacts.
- `processed/` is reserved for normalized or generated datasets produced by future tooling.

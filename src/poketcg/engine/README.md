# engine

Environment-facing boundaries only.

This package will later parse Kaggle/cabt observations, translate legal options into internal domain models, and adapt selected actions back into the runtime format. It should not contain strategy or training logic.

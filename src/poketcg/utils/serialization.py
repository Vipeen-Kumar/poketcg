"""Serialization helper placeholders."""

from __future__ import annotations

from pathlib import Path


def serialize_placeholder(path: Path) -> Path:
    """Placeholder to anchor future serialization helpers."""

    return path

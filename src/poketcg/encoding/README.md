# encoding

Feature and representation abstractions.

This package will later transform domain observations and actions into model-friendly representations. It is intentionally separate from policy and environment code because encoding changes frequently during ML work.
